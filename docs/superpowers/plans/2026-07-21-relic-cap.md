# Cap reliquie a 5 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Limitare le reliquie del giocatore a 5, con scelta di swap sulle 3 fonti dirette (nodo Reliquia, Shop, Altare) e scarto automatico della peggiore sugli eventi.

**Architecture:** Due funzioni pure nel motore (`addRelicWithChoice` per le fonti con UI, `addRelicAutoDrop` per gli eventi). I 4 call-site smettono di fare `[...relics, active]` e chiamano la funzione giusta. Le 3 schermate dirette mostrano un pannello di swap quando sei a 5. Nessun nuovo stato su RunState.

**Tech Stack:** TypeScript, Vitest, React/Next, engine di gioco puro.

## Global Constraints

- **Cap = 5** (`BALANCE.relics.maxRelics`). Vale su tutte e 4 le fonti.
- **NIENTE nuovo stato su RunState** (nessuna scelta pendente): eventi = scarto automatico.
- Il no-op "a 5 senza scelta" deve essere **reference-equal** (ritorna `relics` stesso) così l'harness bot non lo scambia per avanzamento.
- Assegnazione carrier (`assignedTo`/`carrierId` → `corruptOnAssign`) preservata sulla reliquia in ARRIVO.
- Sotto le 5: comportamento **byte-identico** a oggi (append).
- `npm run test` NON typechecka → `npx tsc --noEmit` a parte, ogni task.
- Gate: `npx vitest run tests/engine/campaignBalanceRestricted --disable-console-intercept` (leggi winRate solo con `--disable-console-intercept`). Assert vivo `> 0`.
- Verificare `git rev-parse HEAD` prima di ogni commit (writer git concorrente attivo su questo repo). Master diretto. Commit IT + footer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

### Task 1: Funzioni motore addRelicWithChoice + addRelicAutoDrop (TDD)

Le due funzioni pure che tutti i call-site useranno. Nessun call-site cambiato ancora.

**Files:**
- Modify: `data/constants.ts` (aggiungere `maxRelics: 5` sotto `BALANCE.relics`)
- Modify: `game/engine/relics.ts` (le due funzioni + un helper rarità)
- Test: `tests/engine/relicCap.test.ts` (nuovo)

**Interfaces:**
- Consumes: `ActiveRelic` (`{ relic: Relic; stageObtained: number; assignedTo?: string }`), `RelicRarity` (`'comune' | 'non-comune' | 'rara' | 'epica'`), `BALANCE.relics.maxRelics`.
- Produces:
  ```ts
  export const RELIC_RARITY_ORDER: readonly RelicRarity[] // ['comune','non-comune','rara','epica']
  export function addRelicWithChoice(relics: ActiveRelic[], active: ActiveRelic, replaceId?: string): ActiveRelic[]
  export function addRelicAutoDrop(relics: ActiveRelic[], active: ActiveRelic): ActiveRelic[]
  ```

- [ ] **Step 1: Scrivere i test (rosso)**

Creare `tests/engine/relicCap.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { addRelicWithChoice, addRelicAutoDrop } from '@/game/engine/relics'
import type { ActiveRelic } from '@/types'
import type { RelicRarity } from '@/types/relic'

const mk = (id: string, rarity: RelicRarity, stage = 0): ActiveRelic =>
  ({ relic: { id, name: id, desc: '', rarity, bonus: {} }, stageObtained: stage } as ActiveRelic)

const five = () => [mk('a','comune'), mk('b','comune',1), mk('c','rara',2), mk('d','epica',3), mk('e','non-comune',4)]

describe('addRelicWithChoice', () => {
  it('appends when under the cap', () => {
    const out = addRelicWithChoice([mk('a','comune')], mk('n','rara'))
    expect(out.map(r => r.relic.id)).toEqual(['a','n'])
  })
  it('replaces the chosen relic when at the cap', () => {
    const out = addRelicWithChoice(five(), mk('n','epica'), 'c')
    expect(out).toHaveLength(5)
    expect(out.map(r => r.relic.id)).toEqual(['a','b','n','d','e']) // ordine preservato, c→n
  })
  it('is a reference-equal no-op at the cap without a replaceId', () => {
    const relics = five()
    expect(addRelicWithChoice(relics, mk('n','epica'))).toBe(relics)
  })
  it('is a reference-equal no-op at the cap when replaceId is not owned', () => {
    const relics = five()
    expect(addRelicWithChoice(relics, mk('n','epica'), 'zzz')).toBe(relics)
  })
})

describe('addRelicAutoDrop', () => {
  it('appends when under the cap', () => {
    const out = addRelicAutoDrop([mk('a','comune')], mk('n','rara'))
    expect(out.map(r => r.relic.id)).toEqual(['a','n'])
  })
  it('drops the lowest-rarity relic at the cap', () => {
    // five(): a=comune,b=comune,c=rara,d=epica,e=non-comune → lowest rarity = comune, oldest = a
    const out = addRelicAutoDrop(five(), mk('n','epica'))
    expect(out).toHaveLength(5)
    expect(out.map(r => r.relic.id)).not.toContain('a') // 'a' (comune, più vecchia) scartata
    expect(out.map(r => r.relic.id)).toContain('n')
  })
  it('breaks rarity ties by oldest (lowest stageObtained/index)', () => {
    const relics = [mk('a','comune',5), mk('b','comune',2), mk('c','rara',0), mk('d','rara',1), mk('e','epica',3)]
    const out = addRelicAutoDrop(relics, mk('n','epica'))
    // due comuni: b (stage 2) è più vecchia di a (stage 5) → scarta b
    expect(out.map(r => r.relic.id)).not.toContain('b')
    expect(out.map(r => r.relic.id)).toContain('a')
  })
})
```

Run: `npx vitest run tests/engine/relicCap.test.ts --disable-console-intercept`
Expected: FAIL ("addRelicWithChoice is not a function").

- [ ] **Step 2: Aggiungere maxRelics alla costante**

In `data/constants.ts`, dentro l'oggetto `BALANCE.relics`, aggiungere:
```ts
    maxRelics: 5, // cap reliquie del giocatore (swap/scarto su tutte le fonti)
```
(collocarlo vicino agli altri campi `relics`; verificare la chiave esatta dell'oggetto — `BALANCE.relics`.)

- [ ] **Step 3: Implementare le funzioni in `game/engine/relics.ts`**

Aggiungere in cima (dopo gli import) l'ordine rarità, e in fondo le due funzioni:
```ts
import { BALANCE } from '@/data/constants'
import type { RelicRarity } from '@/types/relic'

/** Rarità dalla peggiore alla migliore — usato per lo scarto automatico. */
export const RELIC_RARITY_ORDER: readonly RelicRarity[] = ['comune', 'non-comune', 'rara', 'epica']

/** Aggiunge una reliquia rispettando il cap, con scelta esplicita di scambio.
 *  Sotto il cap: append. Al cap con replaceId valido: sostituisce (ordine preservato).
 *  Al cap senza replaceId (o id non posseduto): no-op reference-equal (= rifiuto). */
export function addRelicWithChoice(
  relics: ActiveRelic[], active: ActiveRelic, replaceId?: string,
): ActiveRelic[] {
  if (relics.length < BALANCE.relics.maxRelics) return [...relics, active]
  if (!replaceId) return relics
  const idx = relics.findIndex(a => a.relic.id === replaceId)
  if (idx < 0) return relics
  const next = relics.slice()
  next[idx] = active
  return next
}

/** Aggiunge una reliquia scartando automaticamente la peggiore se al cap.
 *  Sotto il cap: append. Al cap: rimuove la rarità più bassa (a parità, la più vecchia =
 *  stageObtained minore, poi indice minore) e aggiunge la nuova in coda. */
export function addRelicAutoDrop(relics: ActiveRelic[], active: ActiveRelic): ActiveRelic[] {
  if (relics.length < BALANCE.relics.maxRelics) return [...relics, active]
  const rank = (r: RelicRarity) => RELIC_RARITY_ORDER.indexOf(r)
  let worst = 0
  for (let i = 1; i < relics.length; i++) {
    const a = relics[i]!, b = relics[worst]!
    const ra = rank(a.relic.rarity), rb = rank(b.relic.rarity)
    if (ra < rb || (ra === rb && (a.stageObtained ?? 0) < (b.stageObtained ?? 0))) worst = i
  }
  return [...relics.slice(0, worst), ...relics.slice(worst + 1), active]
}
```
Se `ActiveRelic` non è già importato/in scope in `relics.ts`, aggiungere l'import da `@/types`.

- [ ] **Step 4: Verde + typecheck**

Run: `npx vitest run tests/engine/relicCap.test.ts --disable-console-intercept && npx tsc --noEmit`
Expected: PASS, tsc exit 0.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "$(cat <<'EOF'
feat(relic): addRelicWithChoice + addRelicAutoDrop — funzioni cap a 5

Due funzioni pure per il cap reliquie: scelta esplicita (fonti con UI) e
scarto automatico della peggiore (eventi). Nessun call-site cambiato ancora.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Eventi usano addRelicAutoDrop

Il flusso più semplice: `events.ts` grantRelic passa da append a scarto automatico. Nessuna UI.

**Files:**
- Modify: `game/engine/events.ts` (case `grantRelic`, riga ~109)
- Test: `tests/engine/relicCapEvents.test.ts` (nuovo) o estendere un test eventi esistente

**Interfaces:**
- Consumes: `addRelicAutoDrop` (Task 1).
- Produces: niente per i task successivi.

- [ ] **Step 1: Test (rosso)**

Creare `tests/engine/relicCapEvents.test.ts` che verifica: partendo da uno state con 5 reliquie e applicando un effetto `grantRelic` (pool `ruleBreaking`), il risultato ha ancora 5 reliquie e contiene la nuova. Modello (adattare agli helper reali di `applyEventEffects` — leggere `game/engine/events.ts` per la firma e `RULE_BREAKING_RELIC_IDS`):
```ts
import { describe, it, expect } from 'vitest'
import { applyEventEffects } from '@/game/engine/events'
import { createRng } from '@/game/engine/rng'
import type { RunState, ActiveRelic } from '@/types'
import type { RelicRarity } from '@/types/relic'

const mk = (id: string, rarity: RelicRarity, stage = 0): ActiveRelic =>
  ({ relic: { id, name: id, desc: '', rarity, bonus: {} }, stageObtained: stage } as ActiveRelic)

function stateWith5(): RunState {
  // minimal RunState with exactly 5 relics; fill required fields as the type demands
  return { /* … leggere types/run.ts per i campi obbligatori … */ relics: [
    mk('a','comune'), mk('b','comune',1), mk('c','rara',2), mk('d','epica',3), mk('e','non-comune',4),
  ] } as unknown as RunState
}

describe('grantRelic rispetta il cap', () => {
  it('a 5 reliquie scarta la peggiore invece di superare il cap', () => {
    const s = stateWith5()
    const { state } = applyEventEffects(s, [{ kind: 'grantRelic', pool: 'ruleBreaking' } as never], createRng(1))
    expect(state.relics).toHaveLength(5)
  })
})
```
Se costruire un RunState minimale è troppo fragile, in alternativa testare a livello del resolver `eventResolver` con un seed che pesca un evento grantRelic (leggere `tests/` esistenti per il pattern di un evento). Preferire il test più robusto disponibile; NON scrivere un test vuoto.

Run: `npx vitest run tests/engine/relicCapEvents.test.ts --disable-console-intercept`
Expected: FAIL (ora `grantRelic` fa append → 6 reliquie).

- [ ] **Step 2: Sostituire l'append con addRelicAutoDrop**

In `game/engine/events.ts`, case `grantRelic` (riga ~109):
```ts
        s = { ...s, relics: addRelicAutoDrop(s.relics, { relic, stageObtained: s.stage }) }
        const dropped = s.relics.every(a => a.relic.id !== /* … */ '') // (vedi sotto per il log)
        log.push(`grantRelic ${pickedId}`)
```
Semplificare: calcolare la lista prima, e loggare se ha scartato:
```ts
        const before = s.relics
        const nextRelics = addRelicAutoDrop(before, { relic, stageObtained: s.stage })
        const droppedId = before.find(a => !nextRelics.includes(a))?.relic.id
        s = { ...s, relics: nextRelics }
        log.push(droppedId ? `grantRelic ${pickedId} (scartata ${droppedId})` : `grantRelic ${pickedId}`)
```
Aggiungere l'import: `import { addRelicAutoDrop } from './relics'` (verificare il path relativo corretto da `events.ts`).

- [ ] **Step 3: Verde + typecheck**

Run: `npx vitest run tests/engine/relicCapEvents.test.ts --disable-console-intercept && npx tsc --noEmit`
Expected: PASS, exit 0. Girare anche gli eventuali test eventi esistenti: `npx vitest run tests/engine/events --disable-console-intercept` (o il path reale) per confermare nessuna regressione.

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "$(cat <<'EOF'
feat(relic): gli eventi rispettano il cap — scartano la reliquia peggiore

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Nodo Reliquia — swap a 5 (choice + resolver + UI)

Aggiungere `replaceRelicId` alla scelta `relic-pick`, farla usare al resolver, e mostrare il pannello swap in `RelicNodeScreen` quando a 5.

**Files:**
- Modify: `game/engine/resolvers/types.ts` (aggiungere `replaceRelicId?` a `relic-pick`)
- Modify: `game/engine/resolvers/recruit.ts` (relicResolver, riga ~59)
- Modify: `hooks/useRunShared.ts` (`chooseRelic`/`pickRelic` firma, riga ~202)
- Modify: `components/screens/RunBRunner.tsx` (onPick wiring, riga ~218)
- Modify: `components/screens/RelicNodeScreen.tsx` (pannello swap)
- Create: `components/relics/RelicSwapPanel.tsx` (componente condiviso)
- Test: `tests/engine/relicCapResolver.test.ts` (nuovo) + `tests/ui/relicSwapPanel.test.tsx` (nuovo)

**Interfaces:**
- Consumes: `addRelicWithChoice` (Task 1).
- Produces:
  - `ResolverChoice` `relic-pick` ora ha `replaceRelicId?: string`.
  - `RelicSwapPanel` component:
    ```ts
    export function RelicSwapPanel({ incoming, owned, onSwap, onReject }: {
      incoming: Relic
      owned: ActiveRelic[]
      onSwap: (replaceRelicId: string) => void
      onReject: () => void
    }): JSX.Element
    ```
    Griglia delle `owned` (cliccabili → `onSwap(id)`) + bottone "Rifiuta" (→ `onReject`).

- [ ] **Step 1: Test resolver (rosso)**

Creare `tests/engine/relicCapResolver.test.ts`: con uno state a 5 reliquie, `relicResolver.resolve` con `{ kind: 'relic-pick', relicId, replaceRelicId }` sostituisce; senza `replaceRelicId` è no-op reference-equal. Leggere `recruit.ts` per come costruire `node`/`state`/`rng` (usare `relicOffer` per un relicId valido dell'offerta). Modello:
```ts
// … costruire state con 5 relics e un relic node; ricavare un relicId dall'offerta …
const withSwap = relicResolver.resolve(state5, node, { kind: 'relic-pick', relicId: offered, replaceRelicId: 'a' }, rng)
expect(withSwap.relics).toHaveLength(5)
const noChoice = relicResolver.resolve(state5, node, { kind: 'relic-pick', relicId: offered }, rng)
expect(noChoice).toBe(state5) // no-op reference-equal a 5 senza scelta
```
Run: `npx vitest run tests/engine/relicCapResolver.test.ts --disable-console-intercept` → FAIL.

- [ ] **Step 2: choice type + resolver**

In `resolvers/types.ts`:
```ts
  | { kind: 'relic-pick'; relicId: string; assignedTo?: string; replaceRelicId?: string }
```
In `recruit.ts` relicResolver.resolve (riga ~57-59), sostituire l'append:
```ts
    const nextRelics = addRelicWithChoice(state.relics, active, choice.replaceRelicId)
    if (nextRelics === state.relics) return state // a 5 senza scelta valida → rifiuto no-op
    const team = choice.assignedTo ? corruptOnAssign(state.team, relic, choice.assignedTo) : state.team
    return { ...state, team, relics: nextRelics, log: [...(state.log ?? []), ev] }
```
Aggiungere `import { addRelicWithChoice } from '../relics'`.

- [ ] **Step 3: hook + RunBRunner wiring**

In `hooks/useRunShared.ts`, la funzione che manda `relic-pick` (riga ~202): aggiungere il parametro `replaceRelicId`:
```ts
  const pickRelic = useCallback((relicId: string, assignedTo?: string, replaceRelicId?: string) => {
    const next = resolveCurrent(runRef.current, { kind: 'relic-pick', relicId, assignedTo, replaceRelicId }, createRng(runRef.current.seed))
    // … resto invariato …
  }, [...])
```
(adeguare il nome reale della callback e il tipo esportato nell'interfaccia del controller.)
In `RunBRunner.tsx` (riga ~218): `onPick={(relicId, assignedTo, replaceRelicId) => c.chooseRelic(relicId, assignedTo, replaceRelicId)}`.

- [ ] **Step 4: RelicSwapPanel (test UI rosso → componente)**

Creare `tests/ui/relicSwapPanel.test.tsx`: rende N pastiglie owned cliccabili + bottone Rifiuta; click su una chiama `onSwap(id)`, click Rifiuta chiama `onReject`. Poi creare `components/relics/RelicSwapPanel.tsx` (riusare lo stile delle pastiglie di RelicBar; ogni owned cliccabile con `data-testid={`swap-${relic.id}`}`; un bottone "Rifiuta" `data-testid="relic-reject"`). Verde.

- [ ] **Step 5: Integrare in RelicNodeScreen**

In `RelicNodeScreen.tsx`: quando `owned.length >= BALANCE.relics.maxRelics` E l'utente ha scelto una reliquia da prendere (`pick`), invece del bottone "Prendi" mostrare `<RelicSwapPanel incoming={pickedRelic} owned={owned} onSwap={(id) => onPick(pick, carrier, id)} onReject={() => /* torna alla scelta o skip */} />`. Sotto le 5, il flusso "Prendi" resta identico. Aggiornare la firma `onPick` a `(relicId, assignedTo?, replaceRelicId?)`.

- [ ] **Step 6: Verde + typecheck**

Run: `npx vitest run tests/engine/relicCapResolver.test.ts tests/ui/relicSwapPanel.test.tsx --disable-console-intercept && npx tsc --noEmit`
Expected: PASS, exit 0.

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "$(cat <<'EOF'
feat(relic): nodo Reliquia — swap quando sei a 5 reliquie

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Shop — swap a 5

Stesso pattern del Task 3 per lo Shop.

**Files:**
- Modify: `game/engine/resolvers/types.ts` (`shop-buy` + `replaceRelicId?`)
- Modify: `game/engine/resolvers/shop.ts` (riga ~56)
- Modify: `hooks/useRunB.ts` (`buyShopItem` opts, riga ~99-105) e `hooks/useRunShared.ts` se la firma è condivisa
- Modify: `components/screens/ShopScreen.tsx` (pannello swap sul relic slot)
- Test: estendere `tests/engine/relicCapResolver.test.ts` con un caso shop, o `tests/engine/shop*.test.ts` esistente

**Interfaces:**
- Consumes: `addRelicWithChoice`, `RelicSwapPanel` (Task 3).
- Produces: `shop-buy` ha `replaceRelicId?: string`.

- [ ] **Step 1: Test (rosso)** — shop-buy di un relic slot a 5 reliquie con `replaceRelicId` sostituisce; senza → no-op. Aggiungere al file resolver test.

- [ ] **Step 2: choice + resolver** — in `types.ts` aggiungere `replaceRelicId?` a `shop-buy`. In `shop.ts` (ramo `slot.kind === 'relic'`, riga ~56) sostituire `[...next.relics, active]` con `addRelicWithChoice(next.relics, active, choice.replaceRelicId)`; se reference-equal a `next.relics` E la lista era piena, non marcare lo slot come comprato e ritornare `state` (rifiuto). Import di `addRelicWithChoice`.

- [ ] **Step 3: hook wiring** — `buyShopItem(slotId, { carrierId?, targetWizardId?, replaceRelicId? })` → passare `replaceRelicId` nella `shop-buy`.

- [ ] **Step 4: ShopScreen** — sul relic slot, quando `relics.length >= maxRelics` e l'utente compra, mostrare `RelicSwapPanel` (onSwap → `onBuy(slotId, { carrierId, replaceRelicId })`, onReject → chiude). Sotto le 5 invariato.

- [ ] **Step 5: Verde + typecheck** — `npx tsc --noEmit` + i test resolver/shop. PASS.

- [ ] **Step 6: Commit**
```bash
git add -A && git commit -m "$(cat <<'EOF'
feat(relic): Shop — swap quando sei a 5 reliquie

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Altare Oscuro — swap a 5

Stesso pattern per l'Altare. Attenzione: l'Altare fa pagare un costo (`applySacrificeCost`) — lo swap NON deve alterare l'ordine costo→aggiunta: prima si paga, poi si aggiunge con swap.

**Files:**
- Modify: `game/engine/resolvers/types.ts` (`altare-buy` + `replaceRelicId?`)
- Modify: `game/engine/resolvers/altare.ts` (riga ~44)
- Modify: `hooks/useRunShared.ts` (`buyAltare` firma, riga ~210)
- Modify: `components/screens/RunBRunner.tsx` (buyAltare wiring) + `components/screens/AltareScreen.tsx` (pannello swap)
- Test: estendere il file resolver test con un caso altare

**Interfaces:**
- Consumes: `addRelicWithChoice`, `RelicSwapPanel`.
- Produces: `altare-buy` ha `replaceRelicId?: string`.

- [ ] **Step 1: Test (rosso)** — altare-buy a 5 reliquie con `replaceRelicId` sostituisce E il costo è comunque pagato una sola volta; senza `replaceRelicId` → no-op reference-equal (e NON deve pagare il costo). Costruire lo state a 5 leggendo `altare.ts`/`altareResolver` per il flusso costo.

- [ ] **Step 2: resolver** — in `altare.ts` (riga ~40-44): dopo `applySacrificeCost` (che ritorna `paid`), calcolare `nextRelics = addRelicWithChoice(paid.relics, active, choice.replaceRelicId)`. Se `nextRelics === paid.relics` (a 5 senza scelta) → **ritornare `state` originale** (non `paid`!) così il costo NON viene applicato (il rifiuto non deve costare nulla). Altrimenti `{ ...paid, team, relics: nextRelics, log }`. Aggiungere l'import.

- [ ] **Step 3: choice + hook** — `replaceRelicId?` su `altare-buy` in types.ts; `buyAltare(relicId, costWizardId?, costRelicId?, carrierId?, replaceRelicId?)` nell'hook e nell'interfaccia controller.

- [ ] **Step 4: AltareScreen** — pannello swap quando a 5 (onSwap → buyAltare con replaceRelicId, onReject → chiude senza pagare). Sotto le 5 invariato.

- [ ] **Step 5: Verde + typecheck** — tsc 0 + test resolver altare PASS.

- [ ] **Step 6: Commit**
```bash
git add -A && git commit -m "$(cat <<'EOF'
feat(relic): Altare — swap a 5 reliquie; il rifiuto non costa nulla

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Verifica finale end-to-end + balance + push

**Files:** nessuna modifica (verifica; eventuali fix di test rotti).

- [ ] **Step 1: Grep — nessun append di reliquia residuo**

Run: `grep -rn "relics: \[\.\.\..*relics" game/engine/ | grep -v "addRelic"`
Expected: vuoto (tutti i 4 call-site passano da addRelicWithChoice/addRelicAutoDrop). Se compare un append diretto residuo, correggerlo.

- [ ] **Step 2: Typecheck + suite intera**

Run: `npx tsc --noEmit && npm run test -- --run --disable-console-intercept 2>&1 | tail -20`
Expected: exit 0, suite verde. Aggiornare eventuali test che assumevano reliquie illimitate.

- [ ] **Step 3: Gate balance**

Run: `npx vitest run tests/engine/campaignBalanceRestricted --disable-console-intercept`
Expected: `winRate > 0` (gate passa). Annotare il valore: se è cambiato rispetto a prima (per via del cap), documentarlo nel report — NON forzare leve senza decisione utente.

- [ ] **Step 4: Verifica visiva (raccomandata)**

Con la skill `run`/Playwright: avviare l'app, arrivare a 5 reliquie (o montare una schermata con 5 owned), confermare che il pannello swap appare sul nodo Reliquia / Shop / Altare e che scambiare funziona; e che sotto le 5 il flusso "Prendi/Compra" è invariato.

- [ ] **Step 5: Push**

```bash
git rev-parse --short HEAD  # HEAD stabile?
git push origin master
```

---

## Self-Review

**Spec coverage:**
- Cap = 5 su tutte e 4 le fonti → Task 2 (eventi) + 3 (nodo) + 4 (shop) + 5 (altare) ✅
- Funzione unica riusata → `addRelicWithChoice`/`addRelicAutoDrop` Task 1, usate in 2-5 ✅
- Swap inline sulle 3 fonti dirette, eventi auto-drop → Task 2 vs 3/4/5 ✅
- No-op reference-equal (bot safe) → Task 1 test + resolver Task 3/4/5 ✅
- Carrier/corruzione preservati → Task 3/4/5 (assignedTo/carrierId sulla reliquia in arrivo) ✅
- Nessun nuovo stato RunState → confermato (nessun campo aggiunto) ✅
- Altare: rifiuto non costa → Task 5 Step 2 (ritorna `state`, non `paid`) ✅
- Balance rimisurato → Task 6 Step 3 ✅

**Placeholder scan:** i test dei Task 2/3/4/5 rimandano a "leggere il file X per costruire lo state" invece di codice completo — è deliberato perché la forma di RunState/dei resolver va letta dal codice reale al momento (costruire uno state fittizio hardcoded sarebbe fragile); ogni step dà il modello, l'asserzione esatta, e il divieto di test vuoti. Le funzioni pure (Task 1) e le firme hanno codice completo.

**Type consistency:** `addRelicWithChoice(relics, active, replaceId?)` e `addRelicAutoDrop(relics, active)` usate con firma coerente in tutti i task; `replaceRelicId` aggiunto in modo uniforme alle 3 choice (`relic-pick`/`shop-buy`/`altare-buy`); `RelicSwapPanel` firma fissata in Task 3 e riusata in 4/5.
